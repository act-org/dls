/**
 * @prettier
 */

import * as React from 'react';

import ButtonPrimary from '.';

export const defaultAppearance = (): React.ReactElement<any> => (
  <ButtonPrimary>Primary</ButtonPrimary>
);

export default { title: 'ButtonPrimary' };
