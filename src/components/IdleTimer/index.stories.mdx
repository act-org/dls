<!-- @prettier -->

import { ArgsTable, Canvas, Meta, Source, Story } from '@storybook/addon-docs';

import { argTypes, Template } from './index.stories.template';
import { IdleTimer } from '.';

<Meta argTypes={argTypes} component={IdleTimer} title="Utilities/IdleTimer" />

## Playground

The `<IdleTimer />` component...

<Canvas isColumn>
  <Source language="jsx" story="Playground" />
  <Story
    name="Playground"
    args={{
      timeoutMs: 1000 * 8,
    }}
  >
    {Template.bind({})}
  </Story>
  <ArgsTable story="Playground" />
</Canvas>
