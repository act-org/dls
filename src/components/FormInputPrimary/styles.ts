/**
 * @prettier
 */

import makeStyles from '~/styles/makeStyles';

export default makeStyles({
  warning: {
    left: '100%',
    marginLeft: 3,
    opacity: 0.5,
    position: 'absolute',
    top: '45%',
  },
});
