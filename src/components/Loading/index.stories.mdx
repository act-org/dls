<!-- @prettier -->

import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Preview,
  Source,
  Story,
} from '@storybook/addon-docs';

import Plus from '~/icons/Plus';

import { argTypes, Template } from './index.stories.template';
import { Loading, LoadingProps } from '.';

<Meta title="Molecules/Loading" component={Loading} argTypes={argTypes} />

<Description of={Loading} />

## Primary

<Canvas>
  <Story
    name="Primary"
    args={{
      circularProgressProps: {
        size: 24,
        thickness: 4.5,
      },
      title: 'Loading...',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Playground

<Canvas isColumn>
  <Source language="jsx" story="Playground" />
  <Story
    name="Playground"
    args={{
      circularProgressProps: {
        size: 24,
        thickness: 4.5,
      },
      title: 'Loading...',
    }}
  >
    {Template.bind({})}
  </Story>
  <ArgsTable story="Playground" />
</Canvas>
