import { Meta, Story, Canvas, Source, Preview, ArgsTable, Description } from '@storybook/addon-docs/blocks';
import { Toolbar, ToolbarProps } from '.';
import { Template, argTypes } from './index.stories.template';

<Meta title="Atoms/Toolbar" component={Toolbar} argTypes={argTypes} parameters={{}} />

<Description of={Toolbar} />

## Primary

<Canvas>
    <Story name="Primary" args={{
    }}>
       {Template.bind({})}
    </Story>
</Canvas>

## Playground

<Canvas isColumn>
    <Source language="jsx" story="Playground" />
    <Story name="Playground" args={{
    }}>
       {Template.bind({})}
    </Story>
    <ArgsTable story="Playground" />
</Canvas>

