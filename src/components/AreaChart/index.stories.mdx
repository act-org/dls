import { Typography } from '@mui/material';
import { ArgsTable, Canvas, Description, Meta, Preview, Source, Story } from '@storybook/addon-docs';

import {
  argTypes,
  Template,
  defaultData,
  defaultAreaKeys,
  largerDataset,
  percentageData,
} from './index.stories.template';
import { AreaChart } from '.';

<Meta title="Molecules/AreaChart" component={AreaChart} argTypes={argTypes} />

<Description of={AreaChart} />

## Default

<Canvas>
  <Story
    name="Default"
    args={{
      areaKeys: ['A'],
      data: defaultData,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With more than one Area

<Canvas>
  <Story
    name="With more than one Area"
    args={{
      areaKeys: ['A', 'B'],
      data: largerDataset,
      areaProps: { fillOpacity: 0.6 },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Custom Colors

<Canvas>
  <Story
    name="Custom Colors"
    args={{
      areaKeys: ['A', 'B'],
      data: largerDataset,
      areaProps: { fillOpacity: 0.6 },
      colors: { A: 'red', B: 'pink' },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With X Label

<Canvas>
  <Story
    name="With X Label"
    args={{
      areaKeys: ['A'],
      data: defaultData,
      xLabel: 'My incredible sample graph',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With Custom X Label

<Canvas>
  <Story
    name="With Custom X Label"
    args={{
      areaKeys: ['A'],
      data: defaultData,
      xLabel: 'My incredible sample graph',
      xLabelProps: { position: 'left', offset: -200 },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With Y Label

<Canvas>
  <Story
    name="With Y Label"
    args={{
      areaKeys: ['A'],
      data: defaultData,
      yLabel: 'Vertical Text',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With Custom X Label

<Canvas>
  <Story
    name="With Custom Y Label"
    args={{
      areaKeys: ['A'],
      data: defaultData,
      yLabel: 'Vertical Text',
      yLabelProps: { position: 'right', angle: 90 },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With Legend

<Canvas>
  <Story
    name="With Legend"
    args={{
      areaKeys: ['A'],
      data: defaultData,
      showLegend: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With Y Reference Line

<Canvas>
  <Story
    name="With Y Reference Line"
    args={{
      areaKeys: ['A'],
      data: defaultData,
      yReferenceValue: 18,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Without Tooltip

<Canvas>
  <Story
    name="Without Tooltip"
    args={{
      areaKeys: ['A'],
      data: defaultData,
      tooltipProps: { cursor: false, wrapperStyle: { visibility: 'hidden' } },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With Custom Tooltip

<Canvas>
  <Story
    name="With Custom Tooltip"
    args={{
      areaKeys: ['A'],
      data: defaultData,
      tooltipProps: {
        content: ({ active, payload, label }) => {
          if (active && payload && payload.length) {
            return (
              <div style={{ backgroundColor: '#FFF' }}>
                <Typography variant="body1">{`${label} : ${payload[0].value}`}</Typography>
                <Typography variant="body2">Anything you want can be displayed here.</Typography>
              </div>
            );
          }
          return null;
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Without Scroll

<Canvas>
  <Story
    name="Without Scroll"
    args={{
      areaKeys: ['A'],
      data: defaultData,
      maxHeight: 1000,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With fixed Height and Width

<Canvas>
  <Story
    name="With fixed Height and Width"
    args={{
      areaKeys: ['A'],
      data: defaultData,
      height: 350,
      width: 600,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## More realistic example

<Canvas>
  <Story
    name="More realistic example"
    args={{
      areaKeys: ['A'],
      data: percentageData,
      colors: { A: '#225479' },
      yReferenceValue: 1,
      yAxisProps: { tickFormatter: v => `${v * 100}%` },
      xLabel: 'Percentage Graph',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Playground

<Source language="jsx" story="Playground" />
<Canvas isColumn>
  <Story
    name="Playground"
    args={{
      areaKeys: ['A'],
      data: defaultData,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
<ArgsTable story="Playground" />
