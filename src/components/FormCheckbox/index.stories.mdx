import { Meta, Story, Canvas, Source, Preview, ArgsTable, Description } from '@storybook/addon-docs/blocks';
import { FormCheckbox, FormCheckboxProps } from '.';
import { Template, argTypes } from './index.stories.template';

<Meta title="Molecules/FormCheckbox" component={FormCheckbox} argTypes={argTypes} />

<Description of={FormCheckbox} />

## Primary

<Canvas>
    <Story name="Primary" args={{
        id: "primaryswitch",
        label: "The label"
    }}>
       {Template.bind({})}
    </Story>
</Canvas>

## Playground

<Canvas isColumn>
    <Source language="jsx" story="Playground" />
    <Story name="Playground" args={{
        id: "playgroundswitch",
        label: "The label"
    }}>
       {Template.bind({})}
    </Story>
    <ArgsTable story="Playground" />
</Canvas>

