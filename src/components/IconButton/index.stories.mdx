import { Meta, Story, Canvas, Source, Preview, ArgsTable, Description } from '@storybook/addon-docs/blocks';
import { IconButton } from '.';
import { Template, argTypes } from './index.stories.template';

<Meta title="Atoms/IconButton" component={IconButton} argTypes={argTypes} parameters={{}} />

<Description of={IconButton} />

## Primary

<Canvas>
    <Story name="Primary" args={{
    }}>
       {Template.bind({})}
    </Story>
</Canvas>

## Playground

<Canvas isColumn>
    <Source language="jsx" story="Playground" />
    <Story inline={false} name="Playground" args={{
    }}>
       {Template.bind({})}
    </Story>
    <ArgsTable story="Playground" />
</Canvas>

