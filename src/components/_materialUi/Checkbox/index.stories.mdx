<!-- @prettier -->

import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Source,
  Story,
} from '@storybook/addon-docs/blocks';
import { Checkbox } from '@material-ui/core';

import { argTypes, Template } from './index.stories.template';

<Meta argTypes={argTypes} component={Checkbox} title="Material UI/Checkbox" />

<Description of={Checkbox} />

## Primary Checkbox

<Canvas>
  <Story
    args={{
      color: 'primary',
      value: 'value',
    }}
    name="Primary"
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Disabled

<Canvas>
  <Story
    args={{
      color: 'primary',
      disabled: true,
      value: 'value',
    }}
    name="Primary: Disabled"
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Secondary Checkbox

<Canvas>
  <Story
    args={{
      color: 'secondary',
      value: 'value',
    }}
    name="Secondary"
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Disabled

<Canvas>
  <Story
    args={{
      color: 'secondary',
      disabled: true,
      value: 'value',
    }}
    name="Secondary: Disabled"
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Indeterminate Checkbox

<Canvas>
  <Story
    args={{
      color: 'primary',
      indeterminate: true,
      value: 'value',
    }}
    name="Indeterminate: Disabled"
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Disabled

<Canvas>
  <Story
    args={{
      color: 'primary',
      disabled: true,
      indeterminate: true,
      value: 'value',
    }}
    name="Indeterminate"
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Checkbox Sizes

### Small

<Canvas>
  <Story
    args={{
      color: 'primary',
      size: 'small',
      value: 'value',
    }}
    name="Size: Small"
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Medium

<Canvas>
  <Story
    args={{
      size: 'medium',
      value: 'value',
    }}
    name="Size: Medium"
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Playground

<Canvas isColumn>
  <Source language="jsx" story="Playground" />
  <Story
    args={{
      checked: true,
      color: 'primary',
      disabled: false,
      size: 'medium',
      value: 'value',
    }}
    name="Playground"
  >
    {Template.bind({})}
  </Story>
  <ArgsTable story="Playground" />
</Canvas>
