/**
 * @prettier
 */

import * as React from 'react';

import ButtonBase from '.';

export const defaultAppearance = (): React.ReactElement<any> => (
  <ButtonBase>Base</ButtonBase>
);

export default { title: 'ButtonBase' };
