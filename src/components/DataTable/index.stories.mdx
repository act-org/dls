<!-- @prettier -->

import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Preview,
  Source,
  Story,
} from '@storybook/addon-docs';
import { Link } from '@material-ui/core';

import PackageVariant from '~/icons/PackageVariant';

import { argTypes, Template } from './index.stories.template';
import { DataTable, DataTableProps } from '.';

<Meta title="Organisms/DataTable" component={DataTable} argTypes={argTypes} />

<Description of={DataTable} />

The `<DataTable />` component is a lightweight alternative to the`<DataGrid />`
component from Material UI.

## Color: Default

<Canvas>
  <Story
    name="Color: Default"
    args={{
      color: 'default',
      totalCount: 10,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Color: Primary

<Canvas>
  <Story
    name="Color: Primary"
    args={{
      color: 'primary',
      totalCount: 10,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Color: Secondary

<Canvas>
  <Story
    name="Color: Secondary"
    args={{
      color: 'secondary',
      totalCount: 10,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Linked Rows

<Canvas>
  <Story
    name="Linked Rows"
    args={{
      RowWrapper: (item, children) => (
        <Link
          href={`https://www.google.com/search?q=${item.name}`}
          style={{
            display: 'contents',
          }}
          target="_blank"
          underline="none"
        >
          {children}
        </Link>
      ),
      totalCount: 10,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Empty State

<Canvas>
  <Story
    name="Empty State"
    args={{
      emptyStateProps: {
        description: 'No Items Found',
        Icon: PackageVariant,
      },
      totalCount: 0,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Paginated

<Canvas>
  <Story
    name="Paginated"
    args={{
      limit: 10,
      offset: 0,
      rowsPerPageOptions: [10, 25, 50],
      totalCount: 100,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Playground

<Canvas isColumn>
  <Source language="jsx" story="Playground" />
  <Story
    name="Playground"
    args={{
      totalCount: 10,
    }}
  >
    {Template.bind({})}
  </Story>
  <ArgsTable story="Playground" />
</Canvas>
